install:
  - mkdir -p clients/java/src/main/resources
  - go build -buildmode=c-shared -o clients/java/src/main/resources/libua-parser-linux-amd64.so ./cmd/cshared/main.go
  - cd clients/java && mvn clean install -DskipTests
